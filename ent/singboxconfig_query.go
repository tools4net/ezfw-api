// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/tools4net/ezfw/backend/ent/predicate"
	"github.com/tools4net/ezfw/backend/ent/singboxconfig"
)

// SingBoxConfigQuery is the builder for querying SingBoxConfig entities.
type SingBoxConfigQuery struct {
	config
	ctx        *QueryContext
	order      []singboxconfig.OrderOption
	inters     []Interceptor
	predicates []predicate.SingBoxConfig
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SingBoxConfigQuery builder.
func (sbcq *SingBoxConfigQuery) Where(ps ...predicate.SingBoxConfig) *SingBoxConfigQuery {
	sbcq.predicates = append(sbcq.predicates, ps...)
	return sbcq
}

// Limit the number of records to be returned by this query.
func (sbcq *SingBoxConfigQuery) Limit(limit int) *SingBoxConfigQuery {
	sbcq.ctx.Limit = &limit
	return sbcq
}

// Offset to start from.
func (sbcq *SingBoxConfigQuery) Offset(offset int) *SingBoxConfigQuery {
	sbcq.ctx.Offset = &offset
	return sbcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (sbcq *SingBoxConfigQuery) Unique(unique bool) *SingBoxConfigQuery {
	sbcq.ctx.Unique = &unique
	return sbcq
}

// Order specifies how the records should be ordered.
func (sbcq *SingBoxConfigQuery) Order(o ...singboxconfig.OrderOption) *SingBoxConfigQuery {
	sbcq.order = append(sbcq.order, o...)
	return sbcq
}

// First returns the first SingBoxConfig entity from the query.
// Returns a *NotFoundError when no SingBoxConfig was found.
func (sbcq *SingBoxConfigQuery) First(ctx context.Context) (*SingBoxConfig, error) {
	nodes, err := sbcq.Limit(1).All(setContextOp(ctx, sbcq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{singboxconfig.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (sbcq *SingBoxConfigQuery) FirstX(ctx context.Context) *SingBoxConfig {
	node, err := sbcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SingBoxConfig ID from the query.
// Returns a *NotFoundError when no SingBoxConfig ID was found.
func (sbcq *SingBoxConfigQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = sbcq.Limit(1).IDs(setContextOp(ctx, sbcq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{singboxconfig.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (sbcq *SingBoxConfigQuery) FirstIDX(ctx context.Context) string {
	id, err := sbcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SingBoxConfig entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SingBoxConfig entity is found.
// Returns a *NotFoundError when no SingBoxConfig entities are found.
func (sbcq *SingBoxConfigQuery) Only(ctx context.Context) (*SingBoxConfig, error) {
	nodes, err := sbcq.Limit(2).All(setContextOp(ctx, sbcq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{singboxconfig.Label}
	default:
		return nil, &NotSingularError{singboxconfig.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (sbcq *SingBoxConfigQuery) OnlyX(ctx context.Context) *SingBoxConfig {
	node, err := sbcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SingBoxConfig ID in the query.
// Returns a *NotSingularError when more than one SingBoxConfig ID is found.
// Returns a *NotFoundError when no entities are found.
func (sbcq *SingBoxConfigQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = sbcq.Limit(2).IDs(setContextOp(ctx, sbcq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{singboxconfig.Label}
	default:
		err = &NotSingularError{singboxconfig.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (sbcq *SingBoxConfigQuery) OnlyIDX(ctx context.Context) string {
	id, err := sbcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SingBoxConfigs.
func (sbcq *SingBoxConfigQuery) All(ctx context.Context) ([]*SingBoxConfig, error) {
	ctx = setContextOp(ctx, sbcq.ctx, ent.OpQueryAll)
	if err := sbcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SingBoxConfig, *SingBoxConfigQuery]()
	return withInterceptors[[]*SingBoxConfig](ctx, sbcq, qr, sbcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (sbcq *SingBoxConfigQuery) AllX(ctx context.Context) []*SingBoxConfig {
	nodes, err := sbcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SingBoxConfig IDs.
func (sbcq *SingBoxConfigQuery) IDs(ctx context.Context) (ids []string, err error) {
	if sbcq.ctx.Unique == nil && sbcq.path != nil {
		sbcq.Unique(true)
	}
	ctx = setContextOp(ctx, sbcq.ctx, ent.OpQueryIDs)
	if err = sbcq.Select(singboxconfig.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (sbcq *SingBoxConfigQuery) IDsX(ctx context.Context) []string {
	ids, err := sbcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (sbcq *SingBoxConfigQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, sbcq.ctx, ent.OpQueryCount)
	if err := sbcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, sbcq, querierCount[*SingBoxConfigQuery](), sbcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (sbcq *SingBoxConfigQuery) CountX(ctx context.Context) int {
	count, err := sbcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (sbcq *SingBoxConfigQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, sbcq.ctx, ent.OpQueryExist)
	switch _, err := sbcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (sbcq *SingBoxConfigQuery) ExistX(ctx context.Context) bool {
	exist, err := sbcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SingBoxConfigQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (sbcq *SingBoxConfigQuery) Clone() *SingBoxConfigQuery {
	if sbcq == nil {
		return nil
	}
	return &SingBoxConfigQuery{
		config:     sbcq.config,
		ctx:        sbcq.ctx.Clone(),
		order:      append([]singboxconfig.OrderOption{}, sbcq.order...),
		inters:     append([]Interceptor{}, sbcq.inters...),
		predicates: append([]predicate.SingBoxConfig{}, sbcq.predicates...),
		// clone intermediate query.
		sql:  sbcq.sql.Clone(),
		path: sbcq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SingBoxConfig.Query().
//		GroupBy(singboxconfig.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (sbcq *SingBoxConfigQuery) GroupBy(field string, fields ...string) *SingBoxConfigGroupBy {
	sbcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SingBoxConfigGroupBy{build: sbcq}
	grbuild.flds = &sbcq.ctx.Fields
	grbuild.label = singboxconfig.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.SingBoxConfig.Query().
//		Select(singboxconfig.FieldName).
//		Scan(ctx, &v)
func (sbcq *SingBoxConfigQuery) Select(fields ...string) *SingBoxConfigSelect {
	sbcq.ctx.Fields = append(sbcq.ctx.Fields, fields...)
	sbuild := &SingBoxConfigSelect{SingBoxConfigQuery: sbcq}
	sbuild.label = singboxconfig.Label
	sbuild.flds, sbuild.scan = &sbcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SingBoxConfigSelect configured with the given aggregations.
func (sbcq *SingBoxConfigQuery) Aggregate(fns ...AggregateFunc) *SingBoxConfigSelect {
	return sbcq.Select().Aggregate(fns...)
}

func (sbcq *SingBoxConfigQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range sbcq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, sbcq); err != nil {
				return err
			}
		}
	}
	for _, f := range sbcq.ctx.Fields {
		if !singboxconfig.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if sbcq.path != nil {
		prev, err := sbcq.path(ctx)
		if err != nil {
			return err
		}
		sbcq.sql = prev
	}
	return nil
}

func (sbcq *SingBoxConfigQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SingBoxConfig, error) {
	var (
		nodes = []*SingBoxConfig{}
		_spec = sbcq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SingBoxConfig).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SingBoxConfig{config: sbcq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, sbcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (sbcq *SingBoxConfigQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := sbcq.querySpec()
	_spec.Node.Columns = sbcq.ctx.Fields
	if len(sbcq.ctx.Fields) > 0 {
		_spec.Unique = sbcq.ctx.Unique != nil && *sbcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, sbcq.driver, _spec)
}

func (sbcq *SingBoxConfigQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(singboxconfig.Table, singboxconfig.Columns, sqlgraph.NewFieldSpec(singboxconfig.FieldID, field.TypeString))
	_spec.From = sbcq.sql
	if unique := sbcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if sbcq.path != nil {
		_spec.Unique = true
	}
	if fields := sbcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, singboxconfig.FieldID)
		for i := range fields {
			if fields[i] != singboxconfig.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := sbcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := sbcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := sbcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := sbcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (sbcq *SingBoxConfigQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(sbcq.driver.Dialect())
	t1 := builder.Table(singboxconfig.Table)
	columns := sbcq.ctx.Fields
	if len(columns) == 0 {
		columns = singboxconfig.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if sbcq.sql != nil {
		selector = sbcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if sbcq.ctx.Unique != nil && *sbcq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range sbcq.predicates {
		p(selector)
	}
	for _, p := range sbcq.order {
		p(selector)
	}
	if offset := sbcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := sbcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SingBoxConfigGroupBy is the group-by builder for SingBoxConfig entities.
type SingBoxConfigGroupBy struct {
	selector
	build *SingBoxConfigQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sbcgb *SingBoxConfigGroupBy) Aggregate(fns ...AggregateFunc) *SingBoxConfigGroupBy {
	sbcgb.fns = append(sbcgb.fns, fns...)
	return sbcgb
}

// Scan applies the selector query and scans the result into the given value.
func (sbcgb *SingBoxConfigGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sbcgb.build.ctx, ent.OpQueryGroupBy)
	if err := sbcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SingBoxConfigQuery, *SingBoxConfigGroupBy](ctx, sbcgb.build, sbcgb, sbcgb.build.inters, v)
}

func (sbcgb *SingBoxConfigGroupBy) sqlScan(ctx context.Context, root *SingBoxConfigQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(sbcgb.fns))
	for _, fn := range sbcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*sbcgb.flds)+len(sbcgb.fns))
		for _, f := range *sbcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*sbcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sbcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SingBoxConfigSelect is the builder for selecting fields of SingBoxConfig entities.
type SingBoxConfigSelect struct {
	*SingBoxConfigQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sbcs *SingBoxConfigSelect) Aggregate(fns ...AggregateFunc) *SingBoxConfigSelect {
	sbcs.fns = append(sbcs.fns, fns...)
	return sbcs
}

// Scan applies the selector query and scans the result into the given value.
func (sbcs *SingBoxConfigSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sbcs.ctx, ent.OpQuerySelect)
	if err := sbcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SingBoxConfigQuery, *SingBoxConfigSelect](ctx, sbcs.SingBoxConfigQuery, sbcs, sbcs.inters, v)
}

func (sbcs *SingBoxConfigSelect) sqlScan(ctx context.Context, root *SingBoxConfigQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sbcs.fns))
	for _, fn := range sbcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sbcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sbcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
